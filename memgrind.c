#include <stdlib.h>
#include "mymalloc.h"

int main() {

	char* ptr = malloc(15*sizeof(char));
//	int* ptr2 = malloc(10*sizeof(int));

//	for(int i = 0; i < 200; i++) {
//		printf("%d \n", mymalloc+i);
//	}

}
